# McUp client — env cho DEV LOCAL (Expo Go). cp .env.example .env rồi sửa.
# Bản build TestFlight/APK KHÔNG đọc file này — env build nằm trong eas.json (profile production/preview).

# Backend khi dev: IP LAN máy chạy uvicorn (iPhone cùng Wi-Fi gọi được).
# iOS simulator: http://localhost:8000 · Android emulator: http://10.0.2.2:8000
EXPO_PUBLIC_API_URL=http://192.168.1.21:8000
